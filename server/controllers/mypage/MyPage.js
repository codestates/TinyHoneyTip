require('dotenv').config();
const { post, post_container, user, sequelize } = require('../../models');
const { QueryTypes } = require('sequelize');

module.exports = {
    getmypage: async (req, res) => {
        // try {
        // }
    },
};
